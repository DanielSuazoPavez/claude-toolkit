{
  "version": "2.72.12",
  "date": "2026-04-27",
  "headline": "require bash 4+ via #!/usr/bin/env bash shebang sweep",
  "skip": false,
  "sections": [
    {
      "kind": "hooks",
      "bullets": [
        "Swept all hook shebangs from `#!/bin/bash` to `#!/usr/bin/env bash` — macOS users with Homebrew bash now pick it up automatically instead of falling back to bash 3.2"
      ]
    },
    {
      "kind": "scripts",
      "bullets": [
        "Same shebang sweep for `setup-toolkit-diagnose.sh` and `lib/profile.sh`"
      ]
    }
  ]
}
