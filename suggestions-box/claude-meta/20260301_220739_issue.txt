Add claude-toolkit bin/ to PATH via symlink in install/setup docs. Currently only works via shell alias (non-interactive shells like Claude Code's Bash tool can't see aliases). Recommended: ln -s ~/projects/personal/claude-toolkit/bin/claude-toolkit ~/.local/bin/claude-toolkit — BUT this breaks TOOLKIT_DIR resolution because the script uses dirname $0 which follows the symlink. Fix: use readlink -f or realpath in the TOOLKIT_DIR line. Could also be part of a 'claude-toolkit install' command. Also: add to dotfiles config.
